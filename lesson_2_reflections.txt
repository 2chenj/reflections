what happens when you intialize a repository ? why do we need to do it ? :
it create a .file which conatin metadata of that ripo
how is the staging area different from the working directory an d the repositroy ? whhat value do youthink it offers ?
the as long as we din't commit the changes doesn't take place in wd and repo btut only in staging area it offers the possibility to choose which file to commit 

how can you use the staging area to make sure you have one commit per logical change ?

u can add changes tpo staging area and commit when u think u have enough logical chnage to commit 

how do the diagrams help you visualise the branch structure?:
it uses different commits diagram means visualise wht is that questions !!

whta si the result of merging two branches together? why do we represent it in the diagram the way we do ?
it regroupes all commit in one log history ! because every commit point is accessible from the marge commit

what are the pros and cons of git's automatic merging vs always doing merges manually ? : it doesn't do all automatically we manually fix conflict case manally atleast we know wht we decided to keep between two modification or the same line fct doc...
